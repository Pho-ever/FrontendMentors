const Navbar = () => {
    return (
        <nav className="navbar">
            <h1>Comment Section</h1>
            <div className="links">
                <a href="/">Home</a>
                <a href="/create">New Comment</a>
            </div>

        </nav>
    );
}

export default Navbar;